<nav class="navbar navbar-expand-lg navbar-dark custom-bg">
  <%= image_tag("darumaW.png", :alt => "logo", :class => 'navbar-brand logo') %>

  <a class="navbar-brand test" href="#">GMIT</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item active ">
          <% if user_signed_in? %>
            <%= link_to 'Sign out', destroy_user_session_path, :class => 'text-light '%>
          <% else %>
            <%= link_to 'Sign in / sign up', new_user_session_path, :class => 'text-light'%> 
          <% end %>

      </li>
    </ul>

  </div>
</nav>